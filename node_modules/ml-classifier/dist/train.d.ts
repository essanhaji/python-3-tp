import * as tf from '@tensorflow/tfjs';
import { IParams, IImageData, IArgs } from './types';
declare const train: (pretrainedModel: tf.Model, data: IImageData, classes: number, params: IParams, args: IArgs) => Promise<{
    model: any;
    history: any;
}>;
export default train;
