import * as tf from '@tensorflow/tfjs';
export interface IImageData {
    data: Uint8ClampedArray;
    width: number;
    height: number;
}
export interface ImageError {
    image: any;
    error: Error;
    index: number;
}
declare const translateImages: (origImages: (string | HTMLImageElement | IImageData | tf.Tensor<tf.Rank>)[], dims: [number, number], origLabels?: string[] | undefined) => Promise<{
    images: (string | HTMLImageElement | IImageData | tf.Tensor<tf.Rank>)[];
    errors: ImageError[];
    labels: string[];
}>;
export default translateImages;
